# Java Array Concatenation Examples

This directory contains Java sample code used in the following article on Code-Izumi:

👉 https://code-izumi.com/

## Covered Topics

- Concatenating arrays using `System.arraycopy`
- Using Stream API (`Stream.concat`)
- Joining array elements into a String (`String.join`)
- Combining arrays via `List` (`ArrayList`, `Collections.addAll`)
- Concatenating `byte[]` using `ByteArrayOutputStream`

## Purpose

These examples are provided as **practical, runnable code** to accompany the article.
Each file focuses on a single approach for clarity and learning purposes.

For full explanations, use cases, and performance considerations, please refer to the blog article.
